// ShopSphere frontend config
// If your backend runs on a different port (e.g. you had to use 5001 because
// 5000 was busy), change API_BASE_URL below to match.
window.SHOPSPHERE_CONFIG = {
  API_BASE_URL: 'http://localhost:5001/api/v1',
};
